// import faker from "faker";

// const MOCK_DATE = faker.date.recent().toISOString();

describe("Health Check", () => {
  it("should be able to return the current date in the time check", () => {
    // jest.spyOn(global.Date, "now").mockImplementationOnce(() => MOCK_DATE);

    expect(true).toBe(true);
  });
});
